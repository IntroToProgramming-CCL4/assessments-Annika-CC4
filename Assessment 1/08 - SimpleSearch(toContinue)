names=["Jake", "Zac", "Ian", "Ron", "Sam", "Dave"]  # Declare list. 
Input= input("Enter name: ").capitalize()          #Ask user to input name. #'.capitalize()' function to ignore capitalization. Function by Geekforgeeks.org. 

if Input in names:                                # if-else statement to check if the given input is in the list.
    print ("Name found.")

else: 
    print ("Name not found.")
